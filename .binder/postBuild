# Enable jupyter_server_proxy
jupyter serverextension enable --sys-prefix jupyter_server_proxy

# Install the dash server extension so that bokeh launches at startup
mv .binder/dash_server_extension.py ${NB_PYTHON_PREFIX}/lib/python*/site-packages/

# Enable bokeh extension
jupyter serverextension enable --sys-prefix dash_server_extension